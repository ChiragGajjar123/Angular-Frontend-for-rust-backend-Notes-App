# Agent Rules & Behavioral Directives

- **STRICT GIT CONTROL**: NEVER perform `git commit` or `git push` directly without explicit permission and review from the user first. Always ask the user for approval before running any git commit or git push commands.
